apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-pvc
  namespace: apps-affine
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
